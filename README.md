
  # simoneteles

  This is a code bundle for simoneteles. The original project is available at https://www.figma.com/design/WE5LLI6TVXPuxxinIvBEZo/simoneteles.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  